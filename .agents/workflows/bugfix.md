# Workflow: Bugfix

1. Read `AGENTS.md` and relevant local `AGENT.md`.
2. Reproduce failure and expected behavior.
3. Identify root cause and minimal fix.
4. Implement fix in `src/`.
5. Add regression test in `tests/`.
6. Summarize validation and impact.
