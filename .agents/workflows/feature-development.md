# Workflow: Feature Development

1. Read `AGENTS.md` and the nearest directory `AGENT.md`.
2. Confirm acceptance criteria and boundaries.
3. Implement a minimal, reviewable change in `src/`.
4. Add or update tests in `tests/`.
5. Update docs if behavior or interfaces changed.
6. Prepare concise review notes.
